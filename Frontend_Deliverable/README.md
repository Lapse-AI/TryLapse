# Dev UI — newest Vision-level dashboard at :8081

Wrapper for `Frontend_V1` · `npm run dev` — **same UI as `npm run dev:vision`**.

```bash
./rehearse serve -o launch-rehearsal/artifacts
npm run dev
# → http://127.0.0.1:8081/
```

See [`UI_PRODUCT_LINES.md`](../enterprise_work_env_simulator_2026/UI_PRODUCT_LINES.md).
